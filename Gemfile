source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.6.3'

gem 'sinatra'
gem 'puma'

group :development, :test do
  gem 'rspec'
end

group :test do
  gem 'rack-test'
  gem 'rspec-benchmark'
end
